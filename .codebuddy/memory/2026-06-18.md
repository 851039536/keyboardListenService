# 2026-06-18

## CODEBUDDY.md 初始化 + README.md 重写

- 创建了 `CODEBUDDY.md`（中文），包含常用命令（build/run/publish）和完整架构说明（双线程模型、四层架构、P/Invoke 详解、WH_KEYBOARD_LL 钩子引擎）
- 全面重写了 `SN_DesktopService/README.md`：
  - 修复了粗体标记（`**` 包围 .NET 9.0）
  - 添加了 ASCII 架构图（双线程模型 + 数据流）
  - 新增项目结构树形图和文件职责表
  - Clarified 钩子引擎的 P/Invoke API 表
  - 补充了权限要求提示（管理员运行）
  - 修复快捷键示例代码（ModifierKeys 按位或组合）
  - 标注了 Form1_Load 中冗余的 `(0,0)` 设置
  - 统一改为 2026-06-18 更新日期
- 项目无单元测试、无 NuGet 依赖，纯 .NET 9.0 Windows Forms 控制台应用

## 全面代码优化（9 项修改）

所有修改通过编译（0 Warning, 0 Error），涉及 5 个文件的改动 + 2 个文档文件同步更新：

### P0 — 安全修复
- **M_GlobalKeyListener.cs**: HookCallback 中缓存 `_hookID` 局部变量避免 Stop() 并发置零；Stop() 延迟置零 `_hookID` 到消息线程退出后
- **M_GlobalKeyListener.cs**: SetHook 失败时通过 Marshal.GetLastWin32Error() 输出错误码并提示以管理员身份运行
- **M_GlobalKeyListener.cs**: 添加 `SN_DesktopService` 命名空间声明（之前为全局命名空间）

### P1 — 代码质量
- **Form1.cs**: 删除 Form1_Load 中的冗余 `(0,0)` 位置设置代码
- **TestService.cs → HotkeyService.cs**: 类重命名为 HotkeyService（去掉测试命名）；改为 `public static` 类；`Start()` 添加 `_started` 防重入标志位
- **HotkeyService.cs**: 订阅 Form1.FormClosed 事件，窗体关闭后自动将 `_form1 = null`，避免持有已释放对象引用

### P2 — 功能增强
- **Program.cs**: 添加 `Console.CancelKeyPress` 事件处理，Ctrl+C 优雅退出时自动调用 `M_GlobalKeyListener.Stop()` 卸载钩子

### P3 — 代码规范
- **keyboardListenService.csproj**: 统一缩进格式（tab → 空格）
- **CODEBUDDY.md + README.md**: 同步更新所有类名引用、命名空间信息、架构图示
